#include "stdafx.h"
#include "mouseEvent.h"

void	  mouseEvent(int button, int state, int x, int y)
{
	if( state == GLUT_UP ) 
	{
		if( button == GLUT_LEFT_BUTTON )
			int i = 0;
	}
	else 
	{
		if( state == GLUT_DOWN ) 
		{
			if( button == GLUT_LEFT_BUTTON )
				int i = 0;
		}
	}
}